In tkinter:
    var:
        TkVersion
        TclVersion
    function:
        _exit(code=0):=Internal function. Calling it will raise the exception SystemExit.
        mainloop(n=0):=Run the main loop of Tcl.
        image_names():=return tk.call('image', 'names'))
        image_types():=return tk.call('image', 'types'))
        Tcl(screenName=None, baseName=None, className='Tk', useTk=0):=  Tk(screenName, baseName, className, useTk)
    class:
        Tk(Misc, Wm)